# Legal document archive

Each published version is stored in its own directory (`<version>/<document>.md`).
Do not edit a published file: add a new version directory and update the current
version in `src/lib/legal/registry.ts`. This keeps existing version URLs and the
text accepted with historical applications immutable.
